appId: com.dataprocessor.app
productName: DataProcessor
directories:
  output: release
  buildResources: build
files:
  - dist
  - dist-electron
win:
  target:
    - target: nsis
  artifactName: ${productName}-${version}-Setup.${ext}
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true